=====================================================

			payam

=====================================================


Dist-IP >>> DistIP.txt

Enter : 
	1) send
		>>> Packet = scapy.all.IP(dst={Target_IP}, src={Your_IP}) / scapy.all.ICMP() / {Message}
		>>> send(Packet)
	
	2) receive
		>>> scapy.all.sniff(count=1, filter="icmp and src host {Your_IP} and dst host {Target_IP}")
		
		'Hello'